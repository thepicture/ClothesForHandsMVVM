<Window x:Class="ClothesForHandsMVVM.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClothesForHandsMVVM"
        xmlns:vm="clr-namespace:ClothesForHandsMVVM.ViewModels"
        xmlns:cv="clr-namespace:ClothesForHandsMVVM.Converters" xmlns:views="clr-namespace:ClothesForHandsMVVM.Views"
        mc:Ignorable="d"
        Title="MainViewTitle"
        Height="850" 
        Width="1200"
        MinWidth="800"
        MinHeight="450"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource GlobalStyle}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:MaterialViewModel}">
            <views:MaterialView></views:MaterialView>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ContentControl Content="{Binding CurrentView}"></ContentControl>
    </Grid>
</Window>
